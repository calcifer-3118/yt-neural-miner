# Required only if you select "Database" mode
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Required for Qwen-VL or Llama if running via specific API providers (optional depending on engine implementation)
OPENAI_API_KEY=
OLLAMA_HOST=http://localhost:11434